# Optional — only for scripts/sprint_review.py.
#
# Deliberately not in requirements.txt: the app itself is FastAPI + stdlib
# sqlite3 and must keep installing and running without any of this. The script
# imports pydantic-ai lazily, so a machine without these still serves the
# planner and still passes the test suite.
#
#   .\.venv\Scripts\python.exe -m pip install -r requirements-ai.txt
#
# Then put the provider key in the environment, never in the database:
#
#   $env:OPENAI_API_KEY = "sk-..."
#
# Swap provider with $env:SPRINT_MODEL — pydantic-ai takes the provider as a
# string prefix, so "anthropic:claude-sonnet-4-6" needs no code change (and the
# matching key, ANTHROPIC_API_KEY).

pydantic-ai>=2.0
