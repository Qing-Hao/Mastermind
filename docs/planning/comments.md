# 1
it seems like the current status of project is not automatic update yet. Previously i already give you how it should behave, there should be a gap to implement that.

# 2 
Map branch currently only can be be diverge 2 times, but not infinitely, I would prefer infinitely as long as i add /. For the colour tone, it just follow the colour but with similar colour tone.

# 3
Table cannot go next line, and i cant implement list or todo list renderer (- or []). It will be helpful to have that.